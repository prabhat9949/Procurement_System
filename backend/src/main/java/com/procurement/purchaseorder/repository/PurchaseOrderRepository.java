package com.procurement.purchaseorder.repository;import com.procurement.purchaseorder.entity.*;import org.springframework.data.jpa.repository.*;import org.springframework.stereotype.Repository;import java.util.*;@Repository public interface PurchaseOrderRepository extends JpaRepository<PurchaseOrder,Long>,JpaSpecificationExecutor<PurchaseOrder>{boolean existsByQuotationComparisonId(Long id);}
